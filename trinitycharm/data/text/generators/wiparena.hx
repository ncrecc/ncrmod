//Floor 1
addfloor('arena_last')
  .setlocation('NCRARENA')
  .addenemies(['Aoife'], [])
  .generate();
  
//Floor 2:
addfloor('arena_last')
  .setlocation('NCRARENA')
  .addenemies(['Aoife'], [])
  .generate();

//Floor 3:
addfloor('arena_last')
  .setlocation('NCRARENA')
  .addenemies(['Aoife'], [])
  .generate();
  
//Floor 4:
addfloor('arena_last')
  .setlocation('NCRARENA')
  .addenemies(['Aoife'], [])
  .generate();
  
//Floor 5:
addfloor('arena_last')
  .setlocation('NCRARENA')
  .addenemies(['Aoife'], [])
  .generate();

//Floor 6:
addfloor('arena_last')
  .setlocation('NCRARENA')
  .addenemies(['Aoife'], [])
  .generate();